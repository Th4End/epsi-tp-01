import { FormatTitrePipe } from './format-titre.pipe';

describe('FormatTitrePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTitrePipe();
    expect(pipe).toBeTruthy();
  });
});
